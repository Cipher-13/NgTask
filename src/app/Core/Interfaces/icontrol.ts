export interface Icontrol { //dedicated for fetching current user
  firstName: string;
  lastName: string;
  email: string;
  age: number;
  image: string;
  phone: string;
  birthDate: string; // ISO string (e.g. "1995-05-23")
}
